<%= form_with(model: game_blog) do |form| %>
  <% if game_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_blog.errors.count, "error") %> prohibited this game_blog from being saved:</h2>

      <ul>
        <% game_blog.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
